{{- if .Values.dashboard.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dynamic-loader.fullname" . }}-dashboard-peers
  labels:
    {{- include "dynamic-loader.labels" . | nindent 4 }}
    component: dashboard
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: {{ include "dynamic-loader.name" . }}-dashboard
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: dds
      port: 7400
      protocol: UDP
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dynamic-loader.fullname" . }}-dashboard-web
  labels:
    {{- include "dynamic-loader.labels" . | nindent 4 }}
    component: dashboard
spec:
  type: {{ .Values.dashboard.service.type | default "NodePort" }}
  selector:
    app: {{ include "dynamic-loader.name" . }}-dashboard
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      {{- if .Values.dashboard.service.httpNodePort }}
      nodePort: {{ .Values.dashboard.service.httpNodePort }}
      {{- end }}
      protocol: TCP
    - name: ws
      port: 9090
      targetPort: 9090
      {{- if .Values.dashboard.service.wsNodePort }}
      nodePort: {{ .Values.dashboard.service.wsNodePort }}
      {{- end }}
      protocol: TCP
{{- end }}
