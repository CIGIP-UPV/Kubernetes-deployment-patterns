imagePullSecrets: []

images:
  camera:
    repository: ghcr.io/cigip-upv/kubernetes-deployment-patterns/ros2-camera
    tag: v1
    pullPolicy: IfNotPresent
  yolo:
    repository: ghcr.io/cigip-upv/kubernetes-deployment-patterns/ros2-yolo
    tag: v1
    pullPolicy: IfNotPresent

network:
  hostNetwork: true

ros:
  domainId: "42"
  rmwImplementation: rmw_cyclonedds_cpp
  localhostOnly: "0"

camera:
  enabled: true
  device: /dev/video0
  width: 1280
  height: 720
  fps: 10
  frameId: camera
  syntheticMode: true
  mountVideoDevice: false
  nodeSelector: {}
  tolerations: []
  affinity: {}
  resources:
    requests:
      cpu: 300m
      memory: 512Mi
    limits:
      cpu: 1000m
      memory: 1Gi

yolo:
  enabled: true
  modelPath: /opt/models/yolov8n.pt
  conf: 0.25
  publishDebugImage: "false"
  publishMetrics: "true"
  nodeSelector: {}
  tolerations: []
  affinity: {}
  resources:
    requests:
      cpu: 1000m
      memory: 2Gi
    limits:
      cpu: 4000m
      memory: 8Gi

# Example split placement:
# camera:
#   nodeSelector:
#     kubernetes.io/hostname: edge-node-1
# yolo:
#   nodeSelector:
#     kubernetes.io/hostname: cloud-node-1
