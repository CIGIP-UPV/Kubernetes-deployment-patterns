{{- if .Values.cyclonedds.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ros2-microservices.fullname" . }}-cyclonedds
  labels:
    {{- include "ros2-microservices.labels" . | nindent 4 }}
data:
  cyclonedds.xml: |
    <CycloneDDS>
      <Domain Id="{{ .Values.ros.domainId }}">
        <General>
          <AllowMulticast>{{ .Values.cyclonedds.allowMulticast | default "false" }}</AllowMulticast>
          <Interfaces>
            <NetworkInterface autodetermine="true"/>
          </Interfaces>
          <Transport>udp</Transport>
        </General>
        <Discovery>
          <ParticipantIndex>auto</ParticipantIndex>
          <Peers>
            {{- if .Values.camera.enabled }}
            <Peer Address="{{ include "ros2-microservices.fullname" . }}-camera-0.{{ include "ros2-microservices.fullname" . }}-camera-peers.{{ .Release.Namespace }}.svc.cluster.local"/>
            {{- end }}
            {{- if .Values.yolo.enabled }}
            <Peer Address="{{ include "ros2-microservices.fullname" . }}-yolo-0.{{ include "ros2-microservices.fullname" . }}-yolo-peers.{{ .Release.Namespace }}.svc.cluster.local"/>
            {{- end }}
            {{- if .Values.dashboard.enabled }}
            <Peer Address="{{ include "ros2-microservices.fullname" . }}-dashboard-0.{{ include "ros2-microservices.fullname" . }}-dashboard-peers.{{ .Release.Namespace }}.svc.cluster.local"/>
            {{- end }}
            {{- range .Values.cyclonedds.extraPeers }}
            <Peer Address="{{ . }}"/>
            {{- end }}
          </Peers>
        </Discovery>
      </Domain>
    </CycloneDDS>
{{- end }}
